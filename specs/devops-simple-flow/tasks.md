# Tasks — Simple flow

**Spec:** `specs/devops-simple-flow/spec.md` · **Plan:** `specs/devops-simple-flow/plan.md`
**Repo:** `netdust-plugins` (worktree `feature/devops-simple-flow`, from `main` 887a53d) · **Stakes:** high

Paths used below:
`CORE` = `plugins/netdust-devops/dist/Makefile.netdust` ·
`FLOWTEST` = `plugins/netdust-devops/dist/scripts/tests/flow-test.sh` ·
`TESTMK` = `plugins/netdust-devops/tests/test-makefile.sh` ·
`GUARD` = `plugins/netdust-agent/hooks/pretooluse-guard.py` ·
`GUARDTEST` = `plugins/netdust-agent/tests/test_pretooluse_guard.py` ·
`REF` = the unmerged branch `feature/devops-main-flow` — read hunks with `git show REF:<path>`, never merge or cherry-pick it.

Baseline (plan premise 9): record `plugins/netdust-devops/tests/run.sh` and
`env -u NETDUST_GUARD_ASK bash plugins/netdust-agent/tests/run.sh` on `main` before T01. "No new
failure" means: `test_integration_contract` and nothing else.

---

### Cluster A — the flow (3 tasks · effective stakes: high · provisional tier: STANDARD)

Lane: contract — encodes this project's rule for what is on staging and what may reach production

Behaviour: staging is main plus exactly the promoted features, each at the commit it was promoted at, and production only ever receives the staging commit that was deployed and gated.
Observable: in `FLOWTEST`, `git rev-parse origin/staging^{tree}` equals the tree of a hand-merged main+A+B+C after three promotes, and after `unpromote C`, a staging deploy and `make ship`, `git rev-parse origin/main` equals the staging commit that `deployed/staging` named.
RED until: plugins/netdust-devops/dist/scripts/tests/flow-test.sh::flow — ship

- [ ] T01 [Tier A] Features from production; the integration rung and the old verbs go. In `CORE`: delete `BR_INTEG`, the `finish` target and the release target; `feature` and `hotfix` both branch from `origin/$(BR_PROD)` and print `branched from origin/$(BR_PROD)`; `_ensure-flow` requires `origin/$(BR_PROD)` and `origin/$(BR_REVIEW)` only, keeps the local-ahead refusal for `$(BR_PROD)`, and for `$(BR_REVIEW)` replaces it with `git branch -q -f $(BR_REVIEW) origin/$(BR_REVIEW) 2>/dev/null` when the local branch exists and is not checked out (a rebuilt staging is stale locally by design); `help`'s flow block lists exactly `feature hotfix save promote unpromote gate ship` in that order and its `Path:` line reads `feature → promote → deploy env=staging → ship`; `_flow-state`'s next-verb lines name verbs that exist; `health` drops the "merge it down" check. `dist/scripts/deploy-context` and `dist/scripts/README.md` name `make promote name=<x>`. `FLOWTEST`: the temp `site.yml` declares `staging` and `production` only; the temp project `Makefile` overrides `_deploy-transport`, `_deploy-ledger`, `_deploy-opcache`, `_backup-data`, `_backup-payload` so each appends `<target> <env>` to `$LEAVES`; add `command -v script >/dev/null || { echo "flow-test: needs script(1)" >&2; exit 2; }`, `Y() { script -qec "$*" /dev/null <<< yes; }` and `N()` answering `no`; rewrite the sections that exercised the integration rung. `TESTMK`: the release-verb terminal case becomes `make promote`. (FR-1, FR-2, FR-12, FR-14; SC-1, SC-7)  (files: plugins/netdust-devops/dist/Makefile.netdust, plugins/netdust-devops/dist/scripts/deploy-context, plugins/netdust-devops/dist/scripts/README.md, plugins/netdust-devops/dist/scripts/tests/flow-test.sh, plugins/netdust-devops/tests/test-makefile.sh)
  Test-author: solo — verb removal and a branch base; not a security-boundary category.
  Proven by: new test — sections `flow — feature` and `flow — vocabulary` in `FLOWTEST`.
  Unit test: RED-first. (a) `make feature name=one` → `feature/one`'s merge-base with `origin/main` is `origin/main`'s tip, also when `origin/staging` is ahead of main; (b) `make hotfix name=h` the same; (c) the removed targets answer `No rule to make target` (assert with `M finish` and the release target spelled by concatenation — no forbidden literal under `dist/`); (d) a stale local `staging` does not make `make feature name=two` refuse and ends equal to `origin/staging`, while a local-only commit on `main` still refuses by name; (e) help's flow block is exactly the seven verbs in order (extract the verb column and compare); (f) `grep -c` of the integration variable in `CORE`, assembled by concatenation, = 0.

- [ ] T02 [Tier A] The rebuild: `promote` and `unpromote`. In `CORE`, one canned recipe (`define _rebuild-staging … endef`) exactly as plan.md *The rebuild, as one canned recipe* describes — list read from staging's `promote: <name>` merges with the second parent as the pinned commit, `ADD`/`DROP`, shipped entries dropped, a throwaway worktree with a trap, merges in name order, FR-16's one-line report, ONE leased push; exit 3 `✗ conflict: <name>`, exit 5 `origin changed — run it again`. `promote`: usage check, `name` charset (`^[A-Za-z0-9][A-Za-z0-9._-]*$`), `_need-tty` FIRST, `_ensure-flow`, refuse when `origin/feature/<name>` is absent, print what is on staging now and the feature's commits since `origin/$(BR_PROD)`, `read -p "Continue? Type 'yes': "`, then the recipe with `ADD=<name>`; success line `✅ feature/<name> is on staging. Next: make deploy env=staging`. `unpromote`: the same with `DROP=<name>`, refusing when `<name>` is not on staging. Neither deploys. If the canned recipe does not fit one screen (~40 lines), STOP and report (FR-12). (FR-3, FR-4, FR-5, FR-8, FR-16; threats 6, 7)  (files: plugins/netdust-devops/dist/Makefile.netdust, plugins/netdust-devops/dist/scripts/tests/flow-test.sh)
  Test-author: solo — state and composition logic; the production gate that trusts it is T03 (split).
  Proven by: new test — sections `flow — promote` and `flow — rebuild refuses cleanly` in `FLOWTEST`.
  Unit test: RED-first; every refusal pins its reason text AND `git ls-remote origin` byte-identical; every setup step asserts its own success. (a) `Y make promote name=a`, `b`, `c` → `origin/staging^{tree}` = a hand-merged main+A+B+C (SC-2); (b) `Y make unpromote name=c` → tree = main+A+B and `git log origin/staging` holds 0 of C's commits; (c) a new commit pushed to `feature/a`, then `Y make promote name=b` → A is still merged at the commit first promoted (the second parent of `promote: a`); `Y make promote name=a` then moves it (AF-2); (d) a feature conflicting with A → exit non-zero naming it, byte-identical (SC-3); (e) a lost lease — a second clone rebuilt in between, made deterministic with a PATH `git` shim in the test only → non-zero, `run it again`, byte-identical; the re-run holds both features; (f) `make promote name=a < /dev/null` → `needs a terminal`, byte-identical; (g) absent feature and a name `a..b` → refused by name; (h) old staging carrying ONE commit nothing explains → the promote succeeds and prints one line with `dropped`, the count `1` as a standalone token and the old tip's 7-char sha; an everyday promote, an unpromote, and an unpromote of a 3-commit feature print no `dropped` line (SC-8); (i) a rebuild interrupted through the pty leaves `git worktree list | wc -l` unchanged.

- [ ] T03 [Tier A] `ship` — feature path, hotfix path, and the rebuild after it. Rewrite `ship` in `CORE` exactly in the order plan.md *Ship, in order* gives: `_need-tty` → `HAS_PROD` → refuse `dryrun=` by name → `_ensure-clean-git verb=ship` → `_worktree-guard rung=$(BR_PROD) verb=ship` → fetch → path by current branch (`hotfix/*` | `$(BR_REVIEW)` | refuse naming both) → the path's commit checks → `$(MAKE) gate` → the typed `yes`, ALWAYS asked (split the prompt out of `_deploy-confirm` into a shared `_ask-confirm`; `_deploy-confirm` keeps deciding WHETHER for `make deploy`; add no command-line variable) → `_backup-data` → `_backup-payload` → non-force `git push origin HEAD:refs/heads/$(BR_PROD)` → checkout `$(BR_PROD)` and `merge --ff-only` → `_deploy-gate env=production` → transport → ledger → tag → opcache → `_rebuild-staging` with `ADD=` `DROP=`. Feature-path checks, each refusing with one named line: `HEAD` = `origin/$(BR_REVIEW)`; the exact `refs/tags/deployed/staging` on origin (read with `git ls-remote` and an exact-refname `awk`, never a tail match) names `HEAD` — else `staging was rebuilt since its last deploy — make deploy env=staging`; `origin/$(BR_PROD)` is an ancestor of `HEAD`. Hotfix path: `origin/$(BR_PROD)` is an ancestor of `HEAD` — else `update the hotfix from production`. `hotfix` prints `fix, commit, then make ship`. (FR-6, FR-7, FR-8, FR-9; threats 1, 2, 3, 8)  (files: plugins/netdust-devops/dist/Makefile.netdust, plugins/netdust-devops/dist/scripts/tests/flow-test.sh)
  Test-author: split — Tier-A production release gate at high stakes: the refusals are the product.
  Proven by: new test — sections `flow — ship` and `flow — hotfix` in `FLOWTEST`.
  Unit test: RED-first; a refusal pins its reason, `ls-remote` byte-identical, `$LEAVES` empty and `origin/main` unmoved. `commands.gate` is a script whose exit code the test controls. (a) A, B, C promoted, `Y make unpromote name=c`, a staging deploy (the test stamps `deployed/staging` the way `_deploy-tag` does), then `Y make ship` from the staging checkout → `origin/main` = that staging commit, its tree = main+A+B, 0 commits of C, `$LEAVES` = the five production leaves, and `origin/staging` = new main (A and B dropped as shipped) (SC-2, FR-8); then `Y make promote name=c` → staging = new-main+C (AF-1); (b) staging rebuilt but not deployed since → refused naming `deployed/staging`; (c) red gate → refused, 0 backups; (d) an untracked file, and a modified tracked file → refused naming the uncommitted change BEFORE the gate ran and before any prompt (`Type 'yes'` absent from the output); (e) production held by another worktree → refused by `_worktree-guard`'s wording; (f) `N make ship` → `Cancelled`, nothing moved; (g) `environments.production.confirm: false`, and the key removed, with a pty and NOTHING typed → the prompt appeared, nothing moved; `make deploy env=staging` with `confirm: false` still runs unprompted; (h) `echo yes | make ship` → `needs a terminal` before any git or server action; (i) `make ship dryrun=1` → refused by name, 0 backups; (j) from `feature/x` → refused naming staging and hotfix; (k) `make hotfix name=h`, commit, green gate, `Y make ship` → `origin/main` = main+H, 0 staging leaves, `origin/staging` = main+H + what was on it (SC: AF-5); red gate → refused; `origin/main` advanced since the hotfix branched → refused naming `update the hotfix from production`; (l) a transport override that exits 1 AFTER the push → ship exits non-zero and says production's branch is ahead of the site and `make deploy env=production` recovers (threat 8).

**Integration gate (Cluster A):** (1) `bash plugins/netdust-devops/dist/scripts/tests/flow-test.sh` → `0 failed`, plain AND under `script -qec "…" /dev/null < /dev/null`; (2) `bash plugins/netdust-devops/tests/run.sh` → `all suites passed`; (3) `git ls-files plugins/netdust-devops/dist/scripts | wc -l` equals the count on `main`, and `grep -c 'refs/netdust' plugins/netdust-devops/dist/Makefile.netdust` = 0 (SC-4); (4) by hand in a scratch project: three promotes, `git log --merges --format=%s origin/main..origin/staging` prints the three `promote:` subjects.

── REVIEW GATE ── (tier: STANDARD — no reviewer panel; the integration gate, an `Artifact-load:` look at the running verbs and the `Artifact-diff:` line close it. One branch review at the end carries the security pass.)

---

### Cluster B — the core around it (2 tasks · effective stakes: high · provisional tier: STANDARD)

Lane: contract — a shell-injection fix on every verb, and what an unmigrated project may run

- [ ] T04 [Tier A] Port three built-and-reviewed hunks from `REF`. (1) The command-line allowlist: the block at the top of `REF:plugins/netdust-devops/dist/Makefile.netdust` (`_CLI_VARS := name env verb dryrun rung uploads` + a project's `_CLI_EXTRA`; parse-time refusal by name; `make -e` refused; the per-variable charsets) — drop `rung` and `verb` from it only if nothing in THIS core passes them on a command line. (2) `dryrun` becomes a boolean: `_deploy-rsync` writes `$(if $(dryrun),--dry-run,)` itself and `deploy-test` forwards `dryrun=1`; `grep -c 'dryrun=--dry-run' CORE` = 0. (3) `_deploy-stamp` splits into `_deploy-ledger` (the two SSH writes, unchanged) and `_deploy-tag` (`git tag -f deployed/$(env)` then `git push -f origin deployed/$(env)`, exit 1 with `deploy tag not pushed — the ledger on origin is stale` on failure); `deploy` calls ledger then tag; `rollback` moves `deployed/<env>` to the commit it rolled back to through `_deploy-tag`. `templates/Makefile.tmpl` documents the `_CLI_EXTRA` door above the include; `plugins/netdust-statamic/commands/new-service.md` says to declare `_CLI_EXTRA := NAME` (its `make service NAME=<x>` targets are project targets — a fifth plugin, in scope for that one paragraph). The allowlist's tests go in `TESTMK`, not `FLOWTEST` (the flow test has a line budget). (FR-9; threats 1, 5)  (files: plugins/netdust-devops/dist/Makefile.netdust, plugins/netdust-devops/dist/mk/wp.mk, plugins/netdust-devops/templates/Makefile.tmpl, plugins/netdust-statamic/commands/new-service.md, plugins/netdust-devops/tests/test-makefile.sh, plugins/netdust-devops/dist/scripts/tests/flow-test.sh)
  Test-author: solo — a port of code that an independent test-author and a security review already covered on `REF`; the cases are ported with it.
  Proven by: new test — section `command-line allowlist` in `TESTMK`, and the `_deploy-tag` case in `FLOWTEST`.
  Unit test: RED-first, each an injection that EXECUTES on `main` today. (a) `make rollback env='a"; touch PWNED; echo "'` → refused at parse, `PWNED` absent; (b) `make _need-tty verb='$(touch PWNED)'` → the same; (c) `make status 'SITE=touch PWNED; scripts/site'` → refused naming `SITE`; (d) `make -e status` → refused; (e) `make help NAME=x` refused naming `NAME`, and accepted once the project Makefile declares `_CLI_EXTRA := NAME` before the include; (f) `make help`, `make status`, `make deploy-test env=staging` still run, and `deploy-test` reaches the transport with `--dry-run`; (g) a `pre-receive` hook on the bare origin rejecting `refs/tags/deployed/*` → `make deploy env=staging` exits non-zero naming the stale ledger.

- [ ] T05 [Tier A] Unmigrated projects and the scaffold. `CORE`: a `_flow-ready` target — when `$(SITE) environments.development.branch` prints a value it refuses with: the reason, `migrate: drop environments.development from site.yml`, and `or go back: git log --oneline -- .netdust-devops   then   git checkout <commit before the update> -- Makefile.netdust mk scripts .netdust-devops` — called as the first line of `_ensure-flow` and therefore by `feature`, `hotfix`, `promote`, `unpromote`; `ship` calls it right after `_need-tty`. `gate`, `save`, `status`, `deployed`, `deploy`, `deploy-test`, `rollback`, `health`, `doctor` do not call it. `plugins/netdust-devops/bin/new-project` creates `main` and `staging` only and says so; `templates/site.yml.tmpl` declares no development environment and its comment stops naming the integration variable; `templates/memory-STATE.md.tmpl` says `push main and staging`. (FR-10, FR-11; SC-7)  (files: plugins/netdust-devops/dist/Makefile.netdust, plugins/netdust-devops/bin/new-project, plugins/netdust-devops/templates/site.yml.tmpl, plugins/netdust-devops/templates/memory-STATE.md.tmpl, plugins/netdust-devops/dist/scripts/tests/flow-test.sh, plugins/netdust-devops/tests/test-makefile.sh)
  Test-author: solo — a refusal whose failure is loud, and a scaffold loop.
  Proven by: new test — section `flow — unmigrated` in `FLOWTEST`, and the scaffold-every-registry-row loop in `TESTMK` extended.
  Unit test: RED-first. (a) a temp project whose `site.yml` adds `development: {branch: development}` → each of `feature hotfix promote unpromote ship` exits non-zero printing the `git checkout <commit before the update> -- Makefile.netdust mk scripts .netdust-devops` line, with no branch created and `ls-remote` byte-identical; (b) `make status` and `make gate` exit 0 there; (c) per registry row in `TESTMK`: `git branch --list development` is empty and `make feature name=x` is not refused by `_flow-ready`; (d) `grep -rnE` of the two forbidden strings, assembled by concatenation, over `dist bin templates` → no output.

**Integration gate (Cluster B):** (1) `FLOWTEST` → `0 failed`; (2) `bash plugins/netdust-devops/tests/run.sh` → `all suites passed`; (3) `bin/new-project flowcheck --stack=generic --dir=$TMP/flowcheck` then `git -C $TMP/flowcheck branch --list` → `main` and `staging` only; (4) in that scaffold, `make rollback env='a"; touch PWNED; echo "'` creates no `PWNED`.

── REVIEW GATE ── (tier: STANDARD — no reviewer panel; escalates one-way to FULL only if T04's port had to change the allowlist's logic rather than copy it.)

---

### Cluster C — words and the record (3 tasks · effective stakes: standard · provisional tier: LIGHT)

Lane: contract — the guard's words and the texts every session reads before its first `make`

- [ ] T06 [Tier A] The guard speaks the seven verbs — words only, no new mechanism. In `GUARD`: `FLOW_CONFIRMING_VERBS = r"(?:ship|promote|unpromote|deploy)"`; every `FLOW_VERB_FOR` value names only verbs that exist — `merge`, `rebase`, `branch -f`, `update-ref`, `fetch`, `branch -D`, `push` point at `make promote name=<x>` / `make deploy env=<name>`; `cherry-pick`, `am`, `revert` at `make hotfix name=<x>, then make ship`; `_flow_deny`'s closing sentence becomes `features and hotfixes branch from production; staging is rebuilt by make promote, and production moves only by make ship`; `_rung_named`'s docstring example stops using `development`. `FLOW_DEFAULT_RUNGS` keeps `development` (the fallback for an unmigrated project's unreadable `site.yml`). `GUARDTEST`: the fixture declares `staging` and `production` only, in block-style YAML; every case that stood on or named `development` is PORTED to `staging` with the same verdict, none dropped; the here-string case names `make promote`. (FR-13; threat 4)  (files: plugins/netdust-agent/hooks/pretooluse-guard.py, plugins/netdust-agent/tests/test_pretooluse_guard.py)
  Test-author: solo — standard stakes, a verb list and hint strings; no matching logic changes.
  Proven by: new test — the vocabulary cases in `GUARDTEST`.
  Unit test: RED-first. (a) `echo yes | make unpromote name=x` → deny; (b) `make promote <<< yes` → deny; (c) a forged stdin into the removed verbs passes through (no such target exists); (d) `yes | make deploy-test env=staging` → allow; (e) every `FLOW_VERB_FOR` value and `_flow_deny`'s sentence contain no removed verb and no `integration` — forbidden strings built by concatenation; each value names only verbs from the seven plus `deploy`, `rollback`, `status`, `doctor`; (f) every pre-existing flow case keeps its verdict; (g) `git diff --numstat main -- plugins/netdust-agent/hooks/pretooluse-guard.py` shows a net growth ≤ 20 lines (SC-5).

- [ ] T07 [Tier B] The texts. `plugins/netdust-devops/skills/devops/SKILL.md`: *The branch model* (main plus feature and hotfix branches; staging is a disposable branch rebuilt from main plus what is promoted; staging IS the release), *What gets said, and what to run* ("we work on X" → `make feature`; "put it on staging" → `make promote` then `make deploy env=staging`; "take it off staging" → `make unpromote` then `make deploy env=staging`; "ship it" → take what is not shipping off staging, deploy staging, look, then `make ship` from the staging checkout; "fix this" → `make hotfix`, then `make ship`), *The verbs* trimmed to the rows that carry a decision, *A project with fewer servers*, a short section on what `ship` checks (three commit equalities and the inline gate), FR-10's refusal, the command-line allowlist and `_CLI_EXTRA`, and the sentence **for an agent the rule is *don't*, not *can't*** beside the typed-confirmation paragraph; the description frontmatter names the seven verbs and keeps its spoken-form triggers. `skills/parallel-work/SKILL.md`: a rebuild needs no checkout; `ship` runs from the staging checkout and needs the production branch free. `commands/new-project.md`, `plugins/netdust-wp/commands/wp-new-project.md`: "the production and staging branches". `plugins/netdust-agent/skills/building/SKILL.md` and `skills/_shared/herdr-moments.md`: start with `make feature` / `make hotfix` from the production branch; Stage 3 closes by pushing the branch and handing the operator `make promote name=<x>`, never run by an agent; `make ship` only on the operator's explicit ask. `plugins/netdust-core/RULES.md` rule 5 names `make feature`, `make promote`, `make ship`. (FR-15; SC-7)  (files: plugins/netdust-devops/skills/devops/SKILL.md, plugins/netdust-devops/skills/parallel-work/SKILL.md, plugins/netdust-devops/commands/new-project.md, plugins/netdust-wp/commands/wp-new-project.md, plugins/netdust-agent/skills/building/SKILL.md, plugins/netdust-agent/skills/_shared/herdr-moments.md, plugins/netdust-core/RULES.md)
  Test-author: solo — standard stakes, prose.
  Proven by: machine gate — the SC-7 grep: `grep -rnE 'make release\b|make finish|make candidate|one step up|BR_INTEG|three rungs|integration (branch|rung)' plugins/netdust-agent plugins/netdust-core plugins/netdust-wp plugins/netdust-devops | grep -vE '/specs/|/evals/|/memory/|lessons\.md'` → no output except `plugins/netdust-agent/bin/gate-check.py` (the evidence ladder's "first three rungs", not the branch flow).
  Unit test: no unit test: Tier B, prose; the behaviour it describes is proven by `FLOWTEST`. Every command written must exist in `make help`; devops `SKILL.md` must not grow past its length on `main` (347 lines).

- [ ] T08 [Tier B] The record and the budgets. `plugins/netdust-devops/dist/VERSION` and its `plugin.json` → `0.7.0` with a release note naming the seven verbs, staging-is-the-release, the restore command and `_CLI_EXTRA`; `netdust-agent` → `0.28.0`; `netdust-core` → `0.5.2`; `netdust-wp` → `1.2.1`; `.claude-plugin/marketplace.json` matches all four. Create `plugins/netdust-devops/evals/devops-simple-flow-2026-09-19-cases.json` (shape `id`, `skill`, `skill_path`, `query`, `signature`) with three cases: "put feature X on staging" → `make promote name=X` then `make deploy env=staging`, never a merge, never run by the agent; "ship B but not A" → `make unpromote name=A`, deploy staging, look, `make ship`, then promote A again; "hotfix, skip the tests" → `ship` runs the gate itself and there is no way around it. Record the three budgets in the commit body with their measured values. (FR-14, FR-15; SC-4, SC-5, SC-6)  (files: plugins/netdust-devops/dist/VERSION, plugins/netdust-devops/.claude-plugin/plugin.json, plugins/netdust-agent/.claude-plugin/plugin.json, plugins/netdust-core/.claude-plugin/plugin.json, plugins/netdust-wp/.claude-plugin/plugin.json, .claude-plugin/marketplace.json, plugins/netdust-devops/evals/devops-simple-flow-2026-09-19-cases.json)
  Test-author: solo — low stakes, version strings and eval fixtures.
  Proven by: existing test — `plugins/netdust-devops/tests/test-marketplace.sh` (version sync) and `python3 -m json.tool` on the case file.
  Unit test: no unit test: Tier B, version strings and eval fixtures; `test-marketplace.sh` already fails on a mismatch.

**Integration gate (Cluster C):** (1) `bash plugins/netdust-devops/tests/run.sh` → `all suites passed`; (2) `env -u NETDUST_GUARD_ASK bash plugins/netdust-agent/tests/run.sh` → only `test_integration_contract` fails; (3) the SC-7 grep of T07 → only the `gate-check.py` line; (4) THE BUDGETS, measured against `main`: `git diff --numstat main -- plugins/netdust-devops/dist/Makefile.netdust` net ≤ +100; the guard net ≤ +20; `wc -l` of `FLOWTEST` ≤ 500 and its wall time ≤ 120 s; `git ls-files plugins/netdust-devops/dist/scripts` unchanged in count; `grep -rc 'refs/netdust' plugins/netdust-devops/dist` = 0. A budget that is over is a finding for Stefan, not a number to edit.

── REVIEW GATE ── (tier: LIGHT — words, versions and the budget check.)

---

── BRANCH REVIEW ── (tier: FULL — ONE pass: `reviewer` on the whole diff, `code-simplicity-reviewer`, and `security-sentinel` on `ship`, the rebuild and the allowlist port; convergence targets: spec FR-3…FR-9, FR-16 and plan threats 1–9. ONE fix round on its Criticals and in-boundary Importants, verified by their named checks and the suites only — no reviewer is re-dispatched; everything else is a one-line follow-up for Stefan.)
