# Session-review — see the skill

This brief moved into the skill, so it works in any repo instead of only this one.

- Decisions: `netdust-core:herdr-orchestration` → `## Role — the session-review pane`
- Mechanics: that skill's `references/session-review.md`

Say "watch the other session" to a fresh agent in a herdr workspace and the skill
carries it. Proposals still land in `memory/session-review/<YYYY-MM-DD>-proposals.md`
of whichever repo the watcher runs in.
