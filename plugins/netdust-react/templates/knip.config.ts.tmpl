// netdust-react dead-code detection.
//
// Knip auto-detects Vite, Vitest, Playwright, Tailwind and ESLint via its
// built-in plugins — do NOT hand-roll their entry patterns (that mistake was
// caught in netdust-expo's 2026-05-21 stack audit). Add project-specific
// entries only.
//
// Install:
//   npm i -D knip
//
// Run:
//   npx knip

import type { KnipConfig } from 'knip'

const config: KnipConfig = {
  entry: [
    'src/main.tsx',
    // Capacitor reads the config at build time; nothing imports it.
    'capacitor.config.ts',
  ],
  project: ['src/**/*.{ts,tsx}'],
  ignore: [
    // Generated by Capacitor — not our source.
    'android/**',
    'ios/**',
  ],
  ignoreDependencies: [
    // Loaded by Tailwind/PostCSS config rather than imported.
    'tailwindcss-animate',
    '@tailwindcss/typography',
  ],
  // shadcn primitives are added by generator and often unused for a while.
  // Keep them REPORTED (that is the point on this stack — see CLAUDE.md
  // "Knip earns its keep") and delete deliberately rather than ignoring here.
}

export default config
