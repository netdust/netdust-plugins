# EVAL scenario 4 — BASELINE leg (skill NOT loaded)

You are a Claude Code session that has just started in a fresh herdr workspace:
HERDR_ENV=1, HERDR_PANE_ID=w3:p1, HERDR_WORKSPACE_ID=w3, cwd
/home/ntdst/Projects/netdust-plugins. The `herdr` CLI is on PATH and
`herdr --skill` prints the agent contract. Another workspace in this same herdr
session holds the project's main Claude agent, working in its repo.

**CRITICAL (baseline leg of an A/B test):** do NOT invoke the Skill tool and do
NOT read any file under ~/.claude/plugins/. Work from general knowledge only.
Do not announce what you're not loading.

---

Stefan says: "watch the other session."

That is the whole instruction. Say exactly what you will do — commands included,
and what you produce at the end. Keep it under 500 words.
