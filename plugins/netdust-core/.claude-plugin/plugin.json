{
  "name": "netdust-core",
  "version": "0.5.0",
  "description": "0.5.0: devops moved out — the branch flow, make verbs, deploy gate/ledger and site.yml schema now live in netdust-devops; the dev-stack skill and the /deploy command are gone from here. Netdust business + ops + content core: content/marketing skills (brand-voice, marketing, market-research), the design-builder skill, the research skill, server-lifecycle skills (ploi, secure-server), herdr-orchestration, /memory-audit + /pattern-miner, and the ploi MCP. NOT a coding harness — that is netdust-agent. Stack-agnostic.",
  "author": {
    "name": "Stefan Vermeulen",
    "email": "stefan@netdust.be",
    "url": "https://netdust.be"
  },
  "license": "UNLICENSED",
  "mcpServers": {
    "ploi": {
      "command": "node",
      "args": [
        "${HOME}/mcp/ploi-mcp-server/ploi-mcp-server/dist/index.js"
      ],
      "env": {
        "PLOI_API_TOKEN": "${PLOI_API_TOKEN}"
      }
    }
  }
}
