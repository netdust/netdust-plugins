# {{PROJECT_NAME}} — STATE

Living snapshot. Read at session start, updated as the session goes.
Injected into every session by the SessionStart hook — what is here is what
the next session knows.

## Current phase

Scaffolded ({{STACK}} / {{TEMPLATE}}). Not yet deployed anywhere.

## Next

- [ ] Fill the `TODO`s in `site.yml` — environment paths, ssh host, state_dir{{PAYLOAD_TODO}}
- [ ] `git remote add origin <url>` and push the three rungs
- [ ] `make doctor` — tools, SSH reachability, payload, devops version

## Decisions

_(a `DECISION:` tag in your notes lands here automatically)_

## Risks

_(a `RISK:` tag lands here)_
