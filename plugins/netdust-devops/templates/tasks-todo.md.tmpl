# {{PROJECT_NAME}} — TODO

The active list for the current branch. A `TODO: …` tag in your notes lands
here automatically.

Keep the checkbox syntax — `- [ ] open`, `- [x] done`. Tools that report
across projects read the unchecked items, and a bullet without a box is
invisible to them.

- [ ] Fill in `site.yml` and run `make doctor`
