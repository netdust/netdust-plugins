# {{PROJECT_NAME}} — TODO

The active list for the current branch. A `TODO: …` tag in your notes lands
here automatically.

The fleet's weekly sweep reads the unchecked items in this file, so keep the
checkbox syntax: `- [ ] open`, `- [x] done`.

- [ ] Fill in `site.yml` and run `make doctor`
