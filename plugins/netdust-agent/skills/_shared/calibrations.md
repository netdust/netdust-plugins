# Calibration index — every war story, told once

**The rule:** a calibration incident has ONE full home (listed below); everywhere else
cites its slug — `(calibration: traverse-clause)` — optionally pointing here. New skill
prose never retells an indexed incident; it cites. This file is an INDEX, not a catalog:
one line of lesson per incident, the full story stays at its canonical home.

All incidents: Folio, Phases 1–3 (2026-05 → 2026-06) unless noted.

## Plan-time gate calibrations

| Slug | Date | Lesson (one line) | Full story |
|---|---|---|---|
| `traverse-clause` | 05-28 | CR-8..11: every route had a guard, no test asserted the denial — cross-tenant reads shipped green; ~80 findings, 7.7× review-to-implementation. Converge visibility into ONE helper; assert denials. | `skills/test-effectiveness/references/failure-modes.md` (mode 5); attack taxonomy in `skills/threat-modeling/SKILL.md` |
| `subphase-b-retrofit` | 05-28 | 7 tasks of BYOK/provider-URL code shipped with no threat model → 2 review rounds, ~30 security findings (SSRF IPv6-mapped bypass, baseUrl exfil). Prevention = 15 min; remediation = 15 min + the rounds. | `skills/threat-modeling/SKILL.md` (calibration data) + its `lessons.md` |
| `drop-workspace-retrofit` | 06-05 | Threat model retrofitted after review on an obviously-triggering surface: 2 rounds / 11 findings vs the proactive phases' 1 round / 3–4. The catalog wasn't the hole; applying it late was. | `skills/planning/SKILL.md` (gate 1a) |
| `class-d-gap` | 06-03 | A one-line `validatePublicUrl` SSRF-guard edit shipped without a threat model — the trigger was keyed to plan-writing only. Ad-hoc security diffs get the gate too (Class D). | `skills/planning/SKILL.md` (gate 1a) + `skills/harnessed-development/SKILL.md` (intake D) |
| `tableview-premise` | 05-30 | "Runs render through the existing TableView" survived spec + plan + handoff and was false; one grep would have falsified it. Ground-truth every reuse-X-for-Y premise (1c). | `skills/planning/SKILL.md` (gate 1c) |
| `teardown-cluster` | 06-05 | A 7-task `__system`-teardown phase behind ONE gate ran flat, merged two tasks into an uncommitted blob, and nearly reviewed irreversible drops beside refactors. Clusters ≤4; irreversible = solo (1f). | `skills/planning/SKILL.md` (task-shaping gate, 1f facet) |
| `tokens-mint-bypass` | 06-01 | The one CRITICAL in a "tight" auth audit was the single write path bypassing the `roleToScopes` convergence point — latent for months. Name the convergence point; the bypass becomes a one-line finding. | `skills/architecture-invariants/SKILL.md` (calibration data) |
| `background-planner-pivots` | 07-04 | Sofie session: plan approved 12:24, invalidated 12:34 ("Sofie has nothing to do with Hermes"), second pivot 13:09 (dev/prod split) — each correction cost a background-agent round-trip (~14 and ~7 min) plus check-on-agent wakeups, while the pre-unattended review ladder caught 4 real loop-breakers that same day (incl. a live-ACL-tested lockout) — the highest-ROI gate of the day. Resolution: presence-aware planning (D3) — plan inline while the human is steering; dispatch background + run the full ladder only before unattended execution. | `skills/planning/SKILL.md` (`<stage_persona>` + seam) |
| `vision-brief-ceremony` | 07-04 | Teacher-app session: a vision-stage brief ("this will become my son's teacher assistant… we don't implement today") was routed into full planning ceremony — the user waited 30+ minutes and asked "still planning?" The deliverable that session needed was shaping/scoping, not a gated plan artifact. Resolution: Class F (D4) — brainstorm-only route (`superpowers:brainstorming` + `refining-ideas`, notes at most, no spec/plan/tasks artifact) with a named promotion path to Class A once the work becomes real. | `skills/harnessed-development/SKILL.md` (intake table + dial + red flags) |

## Execution calibrations

| Slug | Date | Lesson (one line) | Full story |
|---|---|---|---|
| `subphase-a-0of7` | 06-04 | 0/7 subagents re-invoked `testing-workflow` under a weak dispatch one-liner, yet the work was correct — the auditable gate is the structured Test-evidence + STATUS blocks, not a Skill re-invocation. | `skills/building/SKILL.md` (integration, calibration data) |
| `self-grading-split` | 07-04 | The implementer authored its own Tier-A test and self-reported the gating evidence — self-grading: the test drifts to fit the code, the denial path vanishes, a guard gets self-excused to Tier B. Fixed by the test/dev split: an independent `test-author` writes RED from the contract before the implementer greens it, two agents, two commits. | `skills/building/SKILL.md` (`<test_dev_split>` + integration) |
| `tier-conditional-split` | 07-04/05 | The unconditional split (`self-grading-split`'s fix) cost 2× dispatches + 4–6 min sequential latency per task on the run-observability run; its only incident was the test-author's own defective fixtures on T04 (~15 min + 3 extra dispatches to resolve) — not a caught coder-self-grading bug. The cluster review gates + phase-close test-effectiveness audit independently caught all 5 real reproduced defects that run, without the split's help. Resolution: the split became tier-conditional via the machine-checked `Test-author:` field (D1 rule — split iff Tier A **and** a security-boundary category); the no-self-downgrade invariant (no run-time agent may re-decide its own task's mode) keeps the split/solo boundary out of any run-time agent's hands. **Corroborated 08-03 (josworld YOOtheme bridge), with a sharpening.** Five tasks ran split. On the two SECURITY-BOUNDARY tasks it paid for itself outright: T05's author made the meta-prefix trap unshippable (mutation: reintroducing the reference implementation's bug fails 8/16) and found that bare-string schema entries are framework-legal — a shape the controller's own brief had not asked about and that would have been a TypeError white-screen. T07's split produced the `contract-self-contradiction` escalation. On the PURE/MECHANICAL task (T04, a type lookup table) the split bought a good catch but nothing a solo RED-first cycle plus the cluster review would have missed — consistent with D1 already saying so. The controller applied `split` MORE BROADLY than D1 requires; the rule is right, its application drifted. Sharpening for the planner: when a Tier-A task is pure/total/side-effect-free (a mapper, a formatter, a lookup), prefer `solo — Tier A A-lite` and let the cluster review gate carry the independence, reserving `split` for the security-boundary categories D1 actually names. | `skills/building/SKILL.md` (`<test_dev_split>` + integration); `skills/testing-workflow/SKILL.md` ("Who authors vs who greens") |
| `contact-page-8k` | 07-31 | A simple WordPress contact page drew ~8000 lines of tests over hours: Class A pulled the full Stage-3 ladder, `FILES_SECURITY` matched five terms on an ordinary form (nonce/sanitize/capability/role/parse) so every task went Tier A + `split`, the erosion guard had no exit for framework primitives that decide nothing, and nothing in the machine could notice the spend. Resolution: the **stakes dial** (consequence, independent of the class dial), the **evidence ladder** (machine gate > framework > existing test > new test, recorded as `Proven by:`), **presence vs decision** on the erosion guard (prove a primitive is PRESENT; test only predicates encoding a rule this project chose), and `bin/verify-budget.py` as a stakes-scaled HALT. | `skills/testing-workflow/lessons.md` (2026-07-31) |
| `deliverable-last` | 08-03 | **TWO INDEPENDENT SESSIONS, different specs, no shared context, SAME failure shape on the same day — which is what makes it structural rather than a judgement lapse.** Session A (josworld content model): 12 gated tasks of declarative field maps, 290 min, 2077 lines of deliverable behind 4651 lines of tests, before the model was usable. Session B (yootheme-sources bridge), running in parallel: three commits in ~30 min — a plugin, a boot proof, and an ordering proof — with its own commit message stating `ground-truth model-registration ordering BEFORE THE BRIDGE IS BUILT (T03)`. **Neither session had built the thing that was asked for.** Human, watching B live: *"it ground truthed, now its red testing, still not creating the service"* -> *"isn't this an agent problem?"* — it is. ROOT CAUSE, verified by grep, not asserted: `skills/planning/SKILL.md` contains ZERO occurrences of end-to-end / vertical slice / runnable / demoable / "the user can", and the plan-writing gate mandates `## Threat model`, `## Acceptance flows`, `## Architecture invariants` but has no `## First working version`. Every gate in the spine answers *"is this verified?"*; **nothing anywhere asks *"is this useful yet?"*** So the gradient points at proofs, and a faithful agent follows it. Compounding blind spot: `verify-budget.py` is a RATIO (test lines / impl lines), so a task producing ZERO implementation lines cannot fail it — three tasks of pure test infrastructure score perfectly, and Session A passed at 1.39x/1.10x/0.44x while burning four hours. Resolution: a machine gate at the seam (proposed **gate 1j / `check_deliverable_first`**, spec in `skills/planning/references/gate-1j-deliverable-first.md`) requiring the plan to NAME the task producing the first working, demonstrable version and to order it first — mechanically checkable as "does task 1 touch a non-test file whose path the request names?", and it would have FAILED BOTH plans. Prose will not fix this: `stakes-dial-ignored` records the same controller re-running `contact-page-8k` four days after that fix shipped, with the fix installed and read. | `skills/planning/references/gate-1j-deliverable-first.md` (proposal) + `skills/planning/SKILL.md` (Stage 1) |
| `stakes-dial-ignored` | 08-03 | **`contact-page-8k` RECURRED FOUR DAYS AFTER ITS FIX SHIPPED, with the fix installed and read.** josworld content model (4 CPTs + 1 taxonomy + field maps, ~1560 lines of declarative config against an existing DAAN reference): ~3h10m commit span, 19 commits, 6198 lines added, Cluster C not yet started. The plan declared `Stakes: standard`; the controller READ it, RESTATED it verbatim in every dispatch brief, and then ran every task at `high` anyway — per-task implementer + per-task controller mutation test + per-task independent reviewer + per-task findings extraction. Human: *"you ignore the dial, always."* Two diagnostics: **(1) the dial was treated as a field to echo, not a branch to take** — nothing downstream of reading `Stakes:` ever changed shape, so it functioned as documentation; **(2) `verify-budget.py` PASSED throughout (1.39× then 1.10× vs a 2.5× ceiling) because it measures test LINES against implementation lines — it is structurally blind to dispatch count, wall-clock, and reviewer fan-out**, which is where this spend actually went. A clean budget number is not evidence the ceremony was right, and reading it as one is how the recurrence stayed invisible from inside the run. Marginal value was measurable and poor: 6 reviewer dispatches → 2 unique findings; the 5 real defects (a non-biting guard, a stored XSS incl. the controller's own insufficient first fix, two shared-framework bugs, a broken denial) all came from **mutation-testing the guards**, the cheap technique. Resolution: the stakes dial needs a MACHINE consequence at dispatch time (something that refuses or flags a per-task reviewer dispatch on `standard`-stakes declarative work), not another prose instruction to read it — the prose instruction is what already failed here. | `skills/building/SKILL.md` (Step 2.8 fan-out) + `skills/harnessed-development/SKILL.md` (intake dial) |
| `plan-drift-4x` | 05→06 | FOUR consecutive sub-phases (A, C.2, C.3, Phase C) hit plan-vs-source drift caught only by per-task ground-truthing (Step 2.5) — signatures, an entire nonexistent provider API, renamed fields. | `skills/building/SKILL.md` (Step 2.5) |
| `one-cycle-per-bug` | 05-30 | Sub-phase F: bundling review findings I2+I3 into one debug cycle drifted the process even though outcomes were sound. One systematic-debugging invocation per bug. | `skills/building/SKILL.md` (Step 2.7) |
| `sibling-sites` | 05-30 | Sub-phase C.1: every cross-cutting fix had 1–2 sibling sites needing the same change, missed by the primary fix. Enumerate the audit surface in the plan (1e). | `skills/planning/SKILL.md` (gate 1e) |

## Green-but-broken calibrations (the seven failure modes + edge classes)

Nineteen defects reached review/QA **while the unit suite was green** — all
coverage-of-the-dangerous-path escapes. Full per-mode stories:
`skills/test-effectiveness/references/failure-modes.md`.

| Slug | Date | Lesson (one line) | Full story |
|---|---|---|---|
| `compiled-not-rendered` | 08-27 | edushare: a cluster declared the plan's First working version on BUILD-OUTPUT evidence — lessc exit 0, stylesheet md5 changed, all 6 brand hex values present in the compiled CSS. Every claim true; the page had grey headings and square buttons, 5 divergences, 2 of them values the same task had deleted. The artifact WAS loaded and recorded — loading is not comparing. Close a user-facing integration gate on computed values from the running artifact vs the design's own numbers, committed as a test. | `skills/building/lessons.md` + `skills/building/SKILL.md` (Stage 2 integration gate) |
| `stale-fixture-f11` | 05-27 | `author` slug→id changed server-side; legacy fixtures kept three broken UI surfaces green (mode 1). | failure-modes.md mode 1 |
| `migrate-at-boot` | 05-25 | Migration green in the fresh-DB harness, 500 on the long-lived dev DB (mode 2). | failure-modes.md mode 2 |
| `refetch-toggle-blank-editor` | 06-01 | React Query flipped `doc` to `undefined` mid-session; the draft buffer blanked the editor — jsdom never reproduced the toggle (mode 3; also edge class 1, empty/zero). | failure-modes.md mode 3; `skills/feature-acceptance/SKILL.md` edge 1 |
| `unmounted-guard-f1f3` | 05-27 | Scope check present on the MCP path, absent on its HTTP twin; SSE mounted under `wScope` so `requireResource` never ran (mode 4). | failure-modes.md mode 4 |
| `phase-b-seam-blockers` | 06-02 | Two feature-nullifying merge-blockers survived all seven per-task reviews because every test stopped at a seam (mode 4). | failure-modes.md mode 4 |
| `inlineedit-jsdom-race` | 05-23 | Keystroke-before-focus race broke in the real browser; RTL's `userEvent.type` selects-then-types, masking it in every jsdom test (mode 6). Drive UI in the real browser. | failure-modes.md mode 6; `skills/feature-acceptance/SKILL.md` driving layers |
| `bun-sqlite-no-rollback` | — | `db.transaction(async …)` did NOT roll back on an awaited throw at the driver+ORM seam; clients diverged until reload (mode 6; edge class 6, mid-flow failure). | failure-modes.md mode 6; `skills/feature-acceptance/SKILL.md` edge 6 |
| `setinterval-reentrancy` | — | Dispatcher + poller `setInterval` without a re-entrancy latch; passed whenever the scheduler cooperated (mode 7). | failure-modes.md mode 7 |
| `double-submit-a11y-collision` | — | Empty-state CTA and sheet submit shared an accessible name; rapid double-click submitted mid-transition. Single-click tests never reproduce it (edge class 4). | `skills/feature-acceptance/SKILL.md` edge 4 (sole home) |
| `route-vs-service-guard` | — | A route-boundary write guard saw the payload, but schema defaults ran a SECOND mutation pass at the service boundary — incomplete by construction. Drive denials through the full chain (edge class 2). | `skills/feature-acceptance/SKILL.md` edge 2 (sole home) |
| `shape-not-behaviour` | 08-03 | josworld YOOtheme bridge: 19 green tests asserted every emitted resolver reference's SHAPE — literal string, function_exists, not a Closure, with a vacuity guard — and NEVER ONCE CALLED ONE. Two Criticals hid in that gap: an Editor-reachable arbitrary post-meta read (the resolver took its own allow-list from caller-supplied args) and every repeater grid rendering blank (a row has no `ID`, so each cell read post 0). Both reproduced by EXECUTION at review, neither by the suite. When a task emits a CALLABLE REFERENCE, at least one test must invoke it with the emitted args and assert the VALUE. | `skills/test-effectiveness/references/failure-modes.md` (mode 8) |
| `contract-self-contradiction` | 08-03 | josworld T07: an independent test-author committed two mutually-exclusive assertions over one fixture — `assertSame(array_keys(...), ['Case','Team'])` beside `assertArrayHasKey('CaseImpactItem', ...)` on that same array. The implementer reached 16/18, could not satisfy both, and escalated NEEDS_CONTEXT rather than editing a test green; the author then corrected its OWN contract and the correction STRENGTHENED it (+9 assertions, -3, zero methods deleted, replacing a brittle exact-match with the relationship invariant a prior review had said was missing). The protocol worked exactly as designed — a solo agent would have silently relaxed whichever assertion blocked it — but ~25 min and a controller round-trip were avoidable with a self-check before RED_READY. | `skills/building/SKILL.md` (test-author addendum) |
| `docblock-supermajority` | 08-03 | Same session: 62% of the bridge's production lines were comments (`FieldResolver` 127 comment / 69 code; `YOOthemeSourcesService` 337 / 208; `SourcesServiceTest` 452 / 638). The white-page rule was restated VERBATIM in four places. Both reviewers flagged it independently as a Suggestion and the controller did not act. ~40-50 min of a ~1h53m build was prose volume, not verification — invisible to verify-budget.py, which counts test-vs-impl LINES and so reads decision-record prose as implementation. One canonical statement per rule; cross-reference elsewhere. | `skills/building/SKILL.md` (Step 2.6b) |

## Single-home extras (indexed for citation; no dedup needed)

- `gt-gte-race` (05-29), `migrate-journal-idempotent` (05-28), `test-count-delta-13x`
  (06-02), `tier-diagnosis` (06-04) — `skills/testing-workflow/lessons.md`
- `evaluate-stamp-skip` — `commands/evaluate.md`
- `milkdown-ast-not-dom` (05-24), `narrow-key-invalidation` (05-24) —
  `skills/test-effectiveness/references/failure-modes.md`
- `state-md-bloat` (175 KB STATE.md / 100 KB preamble) — `hooks/session-start.sh`
- `mid-sentence-truncation` (06-05) — `hooks/session-stop.py`
