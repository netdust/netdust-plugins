# Implementation Plan: [FEATURE NAME]

> **netdust override template.** This file overrides spec-kit's core `plan-template.md`
> (resolves from `.specify/templates/overrides/`, highest priority). It is spec-kit's
> plan skeleton with the netdust Stage-1 gates baked in, so `/speckit.plan` emits a plan
> that already carries them — turning `harnessed-development` Stage 1 into a freshness
> review and giving `spec-analysis` (Stage 1.5) fixed targets to verify against.
>
> Sections marked **[GATE]** are verified by `spec-analysis`. Do not delete a [GATE]
> heading — if it does not apply, keep the heading and write `N/A — <one-sentence reason>`
> so the omission is explicit and auditable (a missing heading reads as a skipped gate).

**Spec:** `specs/[feature-name]/spec.md`
**Branch:** `[feature-branch]`
**Created:** [DATE]
**Input:** the clarified `spec.md` (all `[NEEDS CLARIFICATION]` resolved — `spec-authoring` Stage 0.5 gate)

## Summary

[1–3 sentences: what this builds and the chosen technical approach. Now the tech stack IS named — that was deferred during /specify.]

## Technical context

- **Stack / language:** [e.g. WordPress PHP 8.2 / Bun+React / Statamic]
- **Primary dependencies:** [frameworks, libraries]
- **Storage / data:** [DB, tables, files]
- **Testing:** [unit runner + e2e runner — must match `testing-workflow` Project Detection]
- **Linked artifacts:** `research.md`, `data-model.md`, `contracts/`, `quickstart.md` (as applicable)

## Constitution check  [GATE]

> The constitution (`/speckit.constitution`, generated by `constitution-bridge` as a view
> over `RULES.md` + `SOUL.md` + `ARCHITECTURE-INVARIANTS.md`) governs this plan. Confirm
> the approach does not violate a non-negotiable before task breakdown.

- [ ] No `RULES.md` non-negotiable is violated (secrets, `vendor/` edits, direct-to-`main`, prod-without-confirmation, global Redis flush…).
- [ ] Coding-standards source named: [link the constitution's standards section the work must satisfy — enforced later by `standards-gate`].
- [ ] Voice/altitude (`SOUL.md`): no unnecessary abstraction/infrastructure introduced ahead of need.

## Threat model  [GATE]

> **BLOCKING and proactive** (harnessed-development 1a). Required BEFORE task breakdown IF
> the feature touches any 1a-trigger surface: user-controlled URLs, auth/session/token,
> untrusted parsing, BYOK credentials, multi-tenancy boundaries, or server-side outbound
> requests to user-supplied addresses. If none match, write `N/A — no 1a-trigger surface
> (<one-line reason>)`. `spec-analysis` fails the plan if a trigger matched and this is N/A.
> Authored via the `threat-modeling` skill; this is its embed point.

### What we're defending
[3–6 concrete assets — not "credentials" but "the apiKey at rest in <table>.<column> with <decrypt-secret>".]

### Who we're defending against
[3–6 actor classes, each marked IN-scope / OUT-of-scope.]

### Attacks → Mitigations (numbered, paired 1:1)
1. **[attack class + specific manifestation]** → **[code-checkable mitigation: named function, file, call sites]**

### Out of scope (explicit deferrals)
- [acknowledged-but-not-mitigated, one-line rationale each]

### How this section is used
- Controller pre-flight verifies each mitigation is in plan-supplied code before dispatch.
- `/code-review` checks against these numbered mitigations (convergence target), not free-form.

## Architecture invariants touched  [GATE]

> harnessed-development 1b. If the plan touches a convergence point named in the project's
> `ARCHITECTURE-INVARIANTS.md` (authorization, data access, live updates, error handling,
> entity modeling), cite it. If the doc does not exist yet and the work touches a
> multi-actor / tenancy / broadcast surface, author it now via `/architecture-invariants
> audit` — at plan-time, not after a bypass ships. If genuinely none, write `N/A — <reason>`.

- Invariant(s) touched: [name them, link the convergence point each routes through]
- [ ] Every fetch/serve/broadcast path in this plan routes through that single convergence point (no re-derived per-site visibility check — the traverse-clause bypass class).

## Spec-premise ground-truth  [GATE]

> harnessed-development 1c. If the approach is "reuse existing infrastructure X for new
> use Y," READ X's source and confirm X accepts Y, BEFORE the plan ships.

- Reuse premise(s): [X reused for Y] — verified against: [file:line read] → [holds / corrected]

## Project structure / file map

[Directories and files this plan creates or changes. Keep it concrete.]

## Phases & review clusters  [GATE]

> harnessed-development 1f + Step 2.8. A plan groups tasks into phases; each phase splits
> into **review clusters of ~3–4 tasks max**. A cluster boundary is a hard STOP
> (`── REVIEW GATE ──`) where execution commits, runs `/integration`, and hands back for
> `/code-review` before the next cluster begins. An **irreversible or security-boundary
> step** (schema drop, teardown migration, auth/token rewrite) gets its **own solo
> cluster** and additionally `/security-review`. The detailed task list lives in
> `tasks.md`; this section declares the cluster shape and its gates.

| Phase | Cluster | Tasks (count ≤4) | Integration gate (what to verify across the cluster) | Extra gate |
|---|---|---|---|---|
| [P1] | C1 | [t1–t3] | [integration assertion] | — |
| [P1] | C2 *(irreversible)* | [t4 alone] | [integration assertion] | `/security-review` |

## Complexity / deviation log

[Any place this plan knowingly deviates from the constitution or adds complexity — with the trade-off named (SOUL.md: name what "best practice" trades off). Empty is good.]

## Progress tracking

- [ ] Constitution check passed
- [ ] Threat model present (or explicit N/A) — verified by `spec-analysis`
- [ ] Invariants cited (or explicit N/A) — verified by `spec-analysis`
- [ ] Spec-premise ground-truthed
- [ ] Review clusters declared with gates (≤4 tasks/cluster; irreversible steps solo) — verified by `spec-analysis`
- [ ] `tasks.md` generated and carries per-task test tiers
