{
  "_readme": "Behavioral eval cases pinning lessons.md entries (netdust-wp skills). Methodology per netdust-agent/evals/EVAL-PLAN.md: baseline (skill off) vs with-skill on the prompt; assertion = the signature behavior appears with-skill. Each case names the lesson it regression-pins. Cases carrying a `baseline_ref` pin a CORRECTNESS re-anchor rather than a lessons.md entry: the baseline is the named commit's skill text, which taught the failure verbatim, so fail-before is a property of the tree and is checkable with the `discriminator` grep rather than argued. NB: run every `discriminator` grep with --exclude-dir=evals — this file quotes the very strings it counts, so including it inflates the `now` column and hides a regression.",
  "cases": [
    {
      "id": "api-rate-budget",
      "skill": "ntdst-architecture",
      "lesson": "Rate-limit budget arithmetic and refusal shapes on the api_data wire (2026-08-10)",
      "prompt": "On an ntdst-core site, I'm adding a public api_data action 'book_slot' that anonymous visitors call from JS via ntdstAPI.call(). I want at most 3 bookings per visitor per minute, so I'll add add_filter('ntdst/api/rate_limit/book_slot', fn() => 3). How many bookings can a first-time visitor actually complete in one 60s window, and how should the handler refuse an over-limit or invalid request?",
      "baseline_expected_failure": "Answers 3 per window; suggests refusing with an apiError()/array payload or ignores the refusal shape entirely.",
      "with_skill_assertion": "States ~2 first-window completions because the get_nonce mint consumes a unit of the SAME action's bucket (mint+call = 2 units; cached nonce = 1 thereafter); refusals must be WP_Error with ['status'], never an apiError() array (which ships HTTP 200 success:true); rate denials surface as 429 rate_limited while auth denials stay 401 — do not collapse them."
    },
    {
      "id": "data-default-key",
      "skill": "ntdst-data",
      "lesson": "'default' field keys are INERT, and select does not enforce its options (2026-08-10)",
      "prompt": "Declare the fields for a new ntdst-core CPT 'ticket': a 'tier' select over exactly gold|silver|bronze that must never store any other value, and an 'active' boolean that should default to true when the editor doesn't touch it.",
      "baseline_expected_failure": "Writes 'active' => ['type' => 'boolean', 'default' => true] and a bare select with 'options', trusting both keys to behave.",
      "with_skill_assertion": "Omits any 'default' key (the vocabulary extracts only required/min/max/validate — a default is inert; omission semantics stated in the description, writers explicit) AND adds a validate closure enforcing the gold|silver|bronze closed set on create and update (select's options are UI-only, not validation)."
    },
    {
      "id": "pages-custom-route",
      "skill": "ntdst-architecture",
      "lesson": "ntdst-core 3.0.0 renamed the whole routing surface with NO aliases and NO shims (re-anchor 2026-08-20)",
      "baseline_ref": "ffcd179 — pre-sweep skill text",
      "prompt": "ntdst-core site, need a shareable card at /share/exhibitions/<slug> that renders a template. Right now it 404s. Write the service — I want the rewrite rule and the route registration, and I need it to actually work on this site, we're on the current ntdst-core.",
      "baseline_expected_failure": "Reaches for ntdst_router()->get() / ntdst_route() / NTDST_Router — the pre-sweep skills named them 45 times and named ntdst_pages()->path() zero times. v3.0.0 removed all three outright with no forwarder, so every one is a fatal at the call site.",
      "with_skill_assertion": "Registers with ntdst_pages()->path('share/exhibitions/:slug', $cb) and never names ntdst_router(), ntdst_route() or NTDST_Router. Pairs the route with an add_rewrite_rule + query_vars registration, and says WHY both are needed: NTDST_Pages matches on REQUEST_URI, so the rewrite exists only to stop WordPress 404ing the URL first. Uses path()'s THIRD argument for any non-GET method rather than ->post() / ->get(), which do not exist.",
      "discriminator": "grep -c 'ntdst_router()->' — 45 pre-sweep, 0 now; 'ntdst_pages()->path(' — 0 pre-sweep, 15 now"
    },
    {
      "id": "rest-cors-gap",
      "skill": "ntdst-architecture",
      "lesson": "The CORS reference was fiction and its advice silently unregistered the route (re-anchor 2026-08-20)",
      "baseline_ref": "ffcd179 — pre-sweep skill text",
      "prompt": "We're building a headless front-end on a different origin that needs to POST submissions to this WordPress site. Add the REST endpoint and lock the CORS down to just our app's origin.",
      "baseline_expected_failure": "Invents NTDST_Cors_Policy (named 18 times pre-sweep) and passes a 'cors' option to the route. Neither exists — and because NTDST_Rest REFUSES any route carrying an option it does not know, the route is never registered at all: it logs _doing_it_wrong and is simply absent from the API. The endpoint 'works' in review and 404s on the wire.",
      "with_skill_assertion": "Registers through ntdst_rest('ns/v1')->post() with a REQUIRED callable 'permission', and passes ONLY options from the closed list (permission, rate_limit, rate_window, args, schema, show_in_index, allow_batch). Names no NTDST_Cors_Policy and no 'cors' option. States plainly that ntdst-core 4.x ships NO CORS handling, that WP core's default rest_send_cors_headers() reflects any Origin with Allow-Credentials, and does NOT hand-roll Access-Control-* headers — it surfaces the gap as a design decision rather than filling it.",
      "discriminator": "grep -c 'NTDST_Cors_Policy' — 18 pre-sweep, 2 now (both retirement notes); \"'cors' =>\" — 1 pre-sweep, 0 now; 'no CORS' — 1 pre-sweep, 9 now"
    },
    {
      "id": "service-disable-filter",
      "skill": "ntdst-architecture",
      "lesson": "The service enable/disable filter is framework-owned and FAILS OPEN on a wrong name (re-anchor 2026-08-20)",
      "baseline_ref": "67e883f — post-sweep skill text, which still taught the project prefix in 3 places",
      "prompt": "The AdminUIService on this ntdst site is loading on the front end and I want it off in staging only. Where do I switch it off, and what exactly do I write in plugin-config.php?",
      "baseline_expected_failure": "Writes a project-prefixed filter — add_filter('stride_admin_ui_enabled', '__return_false') or similar. The pre-sweep AND post-sweep trees both taught {project}_{slug}_enabled in the service template, the hook reference and architecture.md. netdust_{slug}_enabled was the framework's OLD name, retired in the S9/T02 rename with no shim, so any such listener is inert. Worse: it is a DENY filter with a `true` default, so the wrong name FAILS OPEN and the service the user asked to disable keeps booting, silently.",
      "with_skill_assertion": "Names ntdst_service_admin_ui_enabled — the framework-owned, slug-keyed hook — and no project prefix. Derives the slug correctly (AdminUIService -> admin_ui: 'Service' stripped, a run of capitals kept as one token), rather than lowercasing the class name. States that the filter is a DENY filter that fails open, so a misspelled slug boots the service. Offers the three levels (metadata enabled:false / the filter / the ntdst_service_{slug} option) rather than only one.",
      "discriminator": "grep -c '{project}_{slug}_enabled' — 3 pre-sweep, 3 post-sweep, 0 now; 'ntdst_service_' — 2 pre-sweep, 11 post-sweep, 31 now"
    },
    {
      "id": "theme-facade-retired",
      "skill": "ntdst-architecture",
      "lesson": "NTDST_Theme::__call() THROWS — every retired facade wrapper is a call-site fatal (re-anchor 2026-08-20)",
      "baseline_ref": "67e883f — post-sweep skill text, which still taught apiAction() in 9 places incl. the copy-me template",
      "prompt": "Add an AJAX action 'delete_artwork' to our ntdst theme that the admin JS calls. It has to be locked to people who can edit other people's artworks — an Author must not be able to delete someone else's. Wire it up the framework way.",
      "baseline_expected_failure": "Writes $theme->apiAction('delete_artwork', $cb, ['capability' => 'delete_others_posts']). Both the api-handler TEMPLATE and container.md called it live. NTDST_Theme has no such method and no such mixin, and its __call() raises BadMethodCallException for any unknown name — so this throws on boot. Likely also reaches for $theme->register() / $theme->taxonomy() / $theme->module(), all retired the same way.",
      "with_skill_assertion": "Uses ntdst_actions()->register('delete_artwork', $handler, [...]) and does not call any method on $theme outside the five wired mixins (data, pages, response, log, mail). Prefers 'cap_type' over a literal 'capability', giving the reason: the floor is derived from the post type, so a per-type capability map narrows the gate — a literal is correct only while capability_type is still 'post'. States that the floor bites at dispatch ahead of the handler, fails closed on an empty or unresolvable cap, and does not replace the handler's own per-row check.",
      "discriminator": "grep -c 'theme->apiAction(' — 9 pre-sweep, 9 post-sweep, 0 live calls now (5 remaining mentions are all 'RETIRED' notes)"
    },
    {
      "id": "rest-handler-return",
      "skill": "ntdst-architecture",
      "lesson": "NTDST_Rest wraps the PERMISSION, not the handler — there is no envelope and no toRestResponse() (re-anchor 2026-08-20)",
      "baseline_ref": "67e883f — post-sweep skill text, which named toRestResponse() in 4 places",
      "prompt": "Our ntdst_rest() endpoint returns the right rows but the JS client keeps reading response.success as undefined. The handler ends with `return ntdst_response()->withData($rows);`. What's wrong?",
      "baseline_expected_failure": "Tells the caller to add ->toRestResponse(). That method does not exist on NTDST_Response anywhere in ntdst-core — 'Call to undefined method' — so the fix replaces a wrong shape with a fatal. May also assert that NTDST_Rest applies a {success,data} envelope automatically, which it does not.",
      "with_skill_assertion": "Explains that NTDST_Rest wraps only the permission callback; the handler is registered as a plain WP REST callback, so WP core serializes whatever it returns and there is NO automatic envelope — an NTDST_Response object is serialized as its public state. Fixes it with the static builders NTDST_Response::apiSuccessResponse($data) / ::apiErrorResponse($msg, $code, $status), which return a real WP_REST_Response carrying the envelope AND the status. Does not suggest toRestResponse(), and does not suggest calling the protected jsonPayload().",
      "discriminator": "grep -c 'toRestResponse' — 4 pre-sweep, 4 post-sweep, 1 now (the 'there is no toRestResponse()' note)"
    },
    {
      "id": "route-response-refuses",
      "skill": "ntdst-architecture",
      "lesson": "A route callback's NTDST_Response >= 400 REFUSES — WordPress renders its own 404 (re-anchor 2026-08-20)",
      "baseline_ref": "67e883f — post-sweep Return-Values table, which described only the render-and-exit arm",
      "prompt": "On our ntdst_pages()->path() route for /invoices/:id, when the visitor doesn't own the invoice I return ntdst_response()->error('Not yours', 403)->template('errors/denied'). They get a plain WordPress 404 page instead of my denied template. Is the route not matching?",
      "baseline_expected_failure": "Hunts for a route-matching or registration-order bug, or blames the template path — the Return-Values table said an NTDST_Response is simply 'rendered and the request exits', with no status branch, so the observed behaviour looks like the route never ran.",
      "with_skill_assertion": "Identifies the branch, not a bug: resolveRouteResult() splits on getStatus() — 2xx clears the pre-set 404 and renders; >= 400 sends the status, leaves is_404 intact and hands WordPress its OWN not-found template, whether or not the Response names one. States this is deliberate (the route refuses through the output class instead of hand-rolling status_header) and offers the real options — customise the WP 404 template, or return a 2xx Response if a denied page must render from the route.",
      "discriminator": "post-sweep pages.md Return-Values has ONE NTDST_Response row and no '>= 400'; now it has two rows keyed on status"
    }
  ]
}
