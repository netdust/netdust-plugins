# CLAUDE.md — {{PROJECT_NAME}}

You are working on **{{PROJECT_NAME}}**, a Netdust WordPress project. This file is the operating manual — read it at the start of every session.

@~/.claude/plugins/netdust-wp/CLAUDE.md

For operational config (stack, hosting, deploy method, risk level), see `site.yml`.

---

## How to Work in This Repo

1. **For any non-trivial work, load `netdust-core:harnessed-development`** (via the Skill tool). It is the single entry point for the full harness — invoking it engages the whole pipeline so no gate gets skipped: design → write-plan (+ threat-modeling + architecture-invariants when triggered) → execute (subagent/TDD + mandatory testing-workflow at every task close, plus the Step 2.5 plan-freshness ground-truth) → shake-out → finish-branch. It is stack-agnostic and defers to `netdust-wp` for the WordPress-specific skills, reviewers, and test runner. The two gates below are *fired by this skill*; they are documented here as the contract it enforces.

2. **Design before planning uses the framework skills, not generic brainstorming.** On WordPress work, `harnessed-development`'s design stage uses `netdust-wp:ntdst-architecture` (service lifecycle, DI, boundaries — self-triggers on "add a service"), `netdust-wp:ntdst-data` (data layer, CPTs, repositories, REST), and `netdust-wp:ntdst-patterns` (where files live). These replace `superpowers:brainstorming` for WP. There is no `ntdst-brainstorm` skill.

3. **Threat-modeling gate (fired by `harnessed-development` Stage 1a).** `netdust-core:threat-modeling` produces a `## Threat model` section embedded inline in the plan BEFORE task breakdown, IF the work touches: user-controlled input ($_GET/$_POST/REST payloads), auth/capability/nonce surfaces, untrusted parsing (uploads, shortcode/block attrs, AI tool-call args), dynamic SQL, outbound requests to user-supplied URLs, or multi-site boundaries. Also fires on ad-hoc security-boundary edits with no plan (a one-line change to a capability check, a `$wpdb` query, a REST `permission_callback`). `wp-security` and `wp-database` self-trigger on PHP edits and reinforce this.

4. **Architecture-invariants gate (fired by `harnessed-development` Stage 1b).** If the project has an `ARCHITECTURE-INVARIANTS.md`, the work citing a convergence point invokes `netdust-core:architecture-invariants`. `/shakeout` auto-dispatches the `invariant-auditor` (and, on WP, the `ntdst-drift-reviewer`). If the doc doesn't exist yet, author it via `/architecture-invariants audit`.

5. Run tests before committing. Testing is automatic under the harness: `testing-workflow` detects WordPress and selects Codeception/PHPUnit; `wp-testing` covers writing them. `composer lint` (WPCS) must pass.

6. Commit atomically per task. Update `memory/STATE.md` and `tasks/todo.md` as you go.

## Conventions

- **ntdst-core framework conventions apply to all new code** — Modules / Handlers / Admin / Integrations / Contracts / Domain / Infrastructure layering. See `ntdst-architecture` and `ntdst-patterns`.
- **WP-specific rules** live in `~/.claude/plugins/netdust-wp/RULES.md` (sanitize/escape/nonce/cap, `$wpdb->prepare()` always, prefix everything, i18n, never edit WP core or `web/wp/`). Universal rules in `netdust-core/RULES.md`.
- **Stack / hosting / deploy** — see `site.yml`. Deploy via `/deploy` (never to prod without explicit confirmation).

## Project memory

- `memory/STATE.md` — living snapshot of where the project is. Read at session start, update at session end. Keep it a SNAPSHOT — archive history to `memory/ARCHIVE.md` (the session hook warns if STATE.md exceeds ~40 KB).
- `memory/lessons.md` — after any correction, append a rule that prevents the same mistake.
- `tasks/todo.md` — active task list for the current branch / session.

---

**Next step:** open `memory/STATE.md` to see where we left off, then pick the next task.
